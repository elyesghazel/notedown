name: Deployment Notedown - self Hosted
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Deploy to Docker
        run: |
          git config --global --add safe.directory /opt/docker/websites/notedown

          cd /opt/docker/websites/notedown/

          git pull origin main

          docker compose -f docker-compose.yml up -d --build